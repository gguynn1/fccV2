import { PageShell } from "@/components/page-shell";

export function SchedulerRoute() {
  return (
    <PageShell
      title="Scheduler"
      description="Digest windows, reminder timing, and schedule controls will be managed here."
    />
  );
}
