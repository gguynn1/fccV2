import { PageShell } from "@/components/page-shell";

export function ActivityRoute() {
  return (
    <PageShell
      title="Activity"
      description="Dispatch, escalation, and confirmation history will be inspected here in step 43-part-3."
    />
  );
}
