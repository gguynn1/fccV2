import { PageShell } from "@/components/page-shell";

export function EntitiesRoute() {
  return (
    <PageShell
      title="Entities"
      description="Participants, permissions, and thread assignments will be editable inline in step 43-part-2."
    />
  );
}
