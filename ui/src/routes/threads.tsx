import { PageShell } from "@/components/page-shell";

export function ThreadsRoute() {
  return (
    <PageShell
      title="Threads"
      description="Thread membership and configuration will be surfaced here alongside the entities data."
    />
  );
}
