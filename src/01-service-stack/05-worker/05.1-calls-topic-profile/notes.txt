Required tools/dependencies:
- Respect service boundaries: no direct runtime imports from sibling services.
- The topic profile contract that returns tone, format, initiative style, escalation level, and framework grounding.
- Topic-specific response-shaping data before any outbound content is finalized.

Recommended tools/dependencies:
- Mocks for each topic profile so worker tests can isolate composition behavior.
- Golden examples for how topic outputs change message tone and format.

Technologies and integrations:
- Contract: TypeScript interface for the topic profile client — accepts a TopicKey, returns tone/format/initiative/escalation config.
- Implementation: Static TypeScript config objects (one per topic) — no external service call, just a lookup.
- LLM prompt shaping: The returned profile feeds into the Claude API system prompt so generated messages match the topic's voice.
- Testing: Vitest with snapshot assertions comparing profile-shaped outputs across all 12 topics.
