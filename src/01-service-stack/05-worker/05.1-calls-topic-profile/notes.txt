Required tools/dependencies:
- Node.js (LTS) with TypeScript (strict, ESM) for all runtime modules.
- `src/types.ts` for shared enums and cross-service vocabulary.
- Respect service and type boundaries: no direct runtime imports from sibling services.
- The topic profile contract that returns tone, format, initiative style, escalation level, and framework grounding.
- Topic-specific response-shaping data before any outbound content is finalized.

Recommended tools/dependencies:
- `tsc --noEmit` to verify type boundaries.
- `eslint` to keep imports and unused symbols clean.
- `prettier` to keep docs and source formatting consistent.
- `vitest` for fixture-driven verification where the scope has behavior worth testing.
- Mocks for each topic profile so worker tests can isolate composition behavior.
- Golden examples for how topic outputs change message tone and format.

Technologies and integrations:
- This call depends on the topic-profile service contract and `TopicKey` shared vocabulary.
- Anthropic Claude API composition should consume the returned profile fields rather than inventing tone from scratch.
- Vitest snapshots or fixtures are useful for comparing profile-shaped outputs across all 14 topics.
