Required tools/dependencies:
- Node.js (LTS) with TypeScript (strict, ESM) for all runtime modules.
- `src/types.ts` for shared enums and cross-service vocabulary.
- Respect service and type boundaries: no direct runtime imports from sibling services.
- Local `types.ts` in this folder for scope-owned contracts.
- Action-router result types for dispatch, hold, and store outcomes.
- Adapters from final worker decisions into transport, scheduler, or state actions.

Recommended tools/dependencies:
- `tsc --noEmit` to verify type boundaries.
- `eslint` to keep imports and unused symbols clean.
- `prettier` to keep docs and source formatting consistent.
- `vitest` for fixture-driven verification where the scope has behavior worth testing.
- A routing matrix that verifies the correct outcome for each priority level.
- Decision logs that show why an item was sent, batched, or stored.

Technologies and integrations:
- The action router is the typed handoff point into Twilio transport, BullMQ scheduler, or SQLite-backed state.
- Discriminated unions are the right TypeScript fit for dispatch/hold/store outputs.
- pino logs help explain final routing outcomes for debugging and audits.
