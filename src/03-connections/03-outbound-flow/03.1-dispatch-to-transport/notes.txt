Required tools/dependencies:
- Markdown editing and preview.
- ASCII flow diagrams kept aligned with the referenced services.
- No runtime dependencies in this folder; it is documentation-only.
- References to immediate-priority routing and transport delivery behavior.

Recommended tools/dependencies:
- Cross-check wording against the linked service `CLAUDE.md` files.
- Keep terminology aligned with `src/types.ts` and the architecture rules.
- Update diagrams and prose together so the flow stays accurate.
- Examples that keep response-in-place and proactive-routing wording aligned.

Technologies and integrations (documented, not runtime):
- Send mechanism: Twilio Programmable Messaging API — client.messages.create with to/from/body fields.
- Delivery confirmation: Twilio statusCallback webhook reports delivered/failed/undelivered status.
- Response routing: Stays in originating thread — no routing computation needed.
- Proactive routing: Narrowest-thread selection from the routing service's deterministic logic.
- Rate protection: Twilio's built-in rate limits plus budget service's per-thread hourly cap (Redis counters).
