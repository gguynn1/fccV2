Required tools/dependencies:
- Markdown editing and preview.
- ASCII flow diagrams kept aligned with the referenced services.
- No runtime dependencies in this folder; it is documentation-only.
- References to transport, data ingest, scheduler, and queue ownership for all inbound paths.

Recommended tools/dependencies:
- Cross-check wording against the linked service `CLAUDE.md` files.
- Keep terminology aligned with `src/types.ts` and the architecture rules.
- Update diagrams and prose together so the flow stays accurate.
- Flow review against real queue-item fields and source tags.

Technologies and integrations (documented, not runtime):
- Inbound flow documentation should show three doors only: phone messages, email parsing, and scheduled triggers.
- Phone messages arrive via Twilio webhook through the ngrok tunnel to Fastify on localhost:3000.
- Email polling uses imapflow IMAP; reconnects automatically after network outage.
- Scheduled triggers fire from BullMQ repeatable/delayed jobs (Redis AOF required for persistence across restarts).
