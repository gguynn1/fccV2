Required tools/dependencies:
- Markdown editing and preview.
- ASCII flow diagrams kept aligned with the referenced services.
- No runtime dependencies in this folder; it is documentation-only.
- References to the queue, worker, and supporting-service call sequence.

Recommended tools/dependencies:
- Cross-check wording against the linked service `CLAUDE.md` files.
- Keep terminology aligned with `src/types.ts` and the architecture rules.
- Update diagrams and prose together so the flow stays accurate.
- Cross-checks against worker decision order whenever orchestration changes.

Technologies and integrations (documented, not runtime):
- This flow should describe Twilio (via ngrok) plus data-ingest inputs converging on BullMQ (Redis, AOF-persisted), then passing through the worker into transport, scheduler, or state.
- Keep supporting-service references aligned with Anthropic Claude API usage, Redis-backed budget/scheduling, SQLite state, and XState escalation.
- Document stale catch-up: after downtime, the worker checks item relevance before processing — stale items are logged silently.
