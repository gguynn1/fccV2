Required tools/dependencies:
- Markdown editing and preview.
- ASCII flow diagrams kept aligned with the referenced services.
- No runtime dependencies in this folder; it is documentation-only.
- References to transport, identity, and classifier contracts for the inbound front of the stack.

Recommended tools/dependencies:
- Cross-check wording against the linked service `CLAUDE.md` files.
- Keep terminology aligned with `src/types.ts` and the architecture rules.
- Update diagrams and prose together so the flow stays accurate.
- Examples that keep sender resolution and topic classification language in sync.

Technologies and integrations (documented, not runtime):
- Document transport as Twilio input normalization, identity as typed sender resolution, and classification as Anthropic Claude API output validation.
- Keep the described outputs aligned with shared enums in `src/types.ts`.
