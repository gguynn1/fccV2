# The Service Stack

At the top are the people. At the bottom is storage. Everything in between is the engine.

The simplest possible architecture that delivers family coordination outcomes through native text messaging. No apps to install. No channels to manage. No dashboards. Just one messaging address the family can message, optimized for phone-native texting.

Every family member has the assistant saved as a contact on their PHONE. The assistant has one shared messaging identity and no name.

## Simplified Stack

```
PARTICIPANTS
  PARTICIPANT 1, PARTICIPANT 2, PARTICIPANT 3
  each using their phone's messaging app
          |
          |
          v
THREADS
  the assistant has one shared messaging identity
  threads are defined by who is in them

  Private:
    PARTICIPANT 1 + Assistant
    PARTICIPANT 2 + Assistant
    PARTICIPANT 3 + Assistant

  Shared:
    PARTICIPANT 2 + PARTICIPANT 1 + Assistant
    PARTICIPANT 2 + PARTICIPANT 1 + PARTICIPANT 3 + Assistant
          |
          |  all inbound messages,
          |  reactions, images,
          |  and forwarded content
          |  flow down
          |
          v
THE QUEUE
  single funnel for everything

  every item carries:
    - source (who or what sent it)
    - content (raw message or data)
    - who it involves
    - which thread it came from
    - timestamp
          |
          |
          v
THE WORKER
  pulls one item at a time
  runs each item through
  the decision sequence:

    1  classify the topic
    2  identify the entities
    3  determine the action type
    4  check the outbound budget
    5  apply the topic behavior profile
    6  route to the correct thread
    7  dispatch or hold
          |
          |
     _____|_____
    |           |
    v           v
OUTBOUND      SILENT STORAGE
send to       record it but
the right     don't send
thread        anything —
              surface only
              when asked
```

## Detailed Service Flow

```
TRANSPORT LAYER
         |
         |  inbound messages flow down
         |  outbound messages flow up
         |
         v
IDENTITY SERVICE
         |
         |  "PARTICIPANT 1 said this
         |   in the couple thread"
         |
         v
CLASSIFIER SERVICE
         |
         |  "this is a calendar request
         |   about a vet appointment
         |   involving PET"
         |
         v
THE QUEUE
         |
         |  items wait here until
         |  the worker pulls them
         |
         v
WORKER
         |
         |--- calls TOPIC PROFILE SERVICE
         |--- calls ROUTING SERVICE
         |--- calls BUDGET SERVICE
         |--- calls ESCALATION SERVICE
         |--- calls CONFIRMATION SERVICE
         |
         |  once all decisions are made
         |
         v
ACTION ROUTER
         |
    _____|______________
   |          |         |
   v          v         v
DISPATCH   HOLD      STORE
send now   batch     record in
via the    for next  state but
Transport  digest    send nothing
Layer      or quiet  — surface
(back up   window    only when
to top)              asked
```

## System Promises

The assistant never sends more than it should. Outbound is budgeted, batched, and routed to the narrowest audience.

The assistant never acts without permission on sends, financial actions, or system changes.

The assistant always responds where you are. Proactive messages go to the narrowest thread that fits.

The assistant shifts tone and behavior by topic while remaining one coherent identity.

Every source of information flows through the same queue, same worker, same dispatch logic.

The design is testable. Every behavior maps to a scenario that can be generated, evaluated, and used to refine the model.

One phone number. One queue. One set of rules. Infinite scenarios to prove it.

## Scenario Testing Framework

The system must support infinite scenario generation and evaluation against the model's rules. Scenarios are the test harness for the design, not just the software.

Every scenario has five parts:

- **Trigger** — what starts it (human message, scheduled event, email, calendar change, or combination).
- **Expected dispatch** — who gets what, in which thread, at what priority, in what tone, and when.
- **Expected input handling** — what responses might come and what each means.
- **Escalation check** — if expected responses don't arrive, does the right escalation happen?
- **Collision check** — does this scenario's outbound conflict with other active scenarios?

Scenarios are generated by combining a random topic, trigger type, participant set, and complication (no response, conflicting events, multi-step, rescheduled, ambiguous input, concurrent scenarios). Failures refine the model — the topic behavior profiles, routing rules, escalation paths, and batching thresholds.
